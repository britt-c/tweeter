<% @user.each do |user| %>
  <p>
    <%= user.username %>
    -
    <%= link_to("edit", edit_user_path(user.id)) %>
    -
    <%= link_to("delete", user_path(user.id), method: :delete, data: {confirm: "Are you sure you want to do this?1!?!?!?!!!?!?!"})%>
  </p>
<% end %>

